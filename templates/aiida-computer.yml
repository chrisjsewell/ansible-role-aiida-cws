label: "{{ _aiida_computer_label }}"
hostname: "{{ _aiida_computer_hostname }}"
transport: "{{ _aiida_computer_transport }}"
scheduler: "{{ _aiida_computer_scheduler }}"
work_dir: "{{ _aiida_computer_work_dir }}"
mpirun_command: "{{ _aiida_computer_mpirun }}"
mpiprocs_per_machine: "{{ _aiida_computer_default_mpiprocs }}"
